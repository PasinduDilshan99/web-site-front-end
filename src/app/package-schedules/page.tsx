import PackageSchedulePage from "@/pages/PackageSchedulePage";
import React from "react";

const page = () => {
  return (
    <div>
      <PackageSchedulePage />
    </div>
  );
};

export default page;
